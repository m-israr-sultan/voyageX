import { Region } from '@prisma/client';

export const NORTHERN_REGIONS: Region[] = [
  Region.HUNZA,
  Region.SKARDU,
  Region.GILGIT,
  Region.NAGAR,
  Region.GHIZER,
  Region.SWAT,
  Region.KALAM,
  Region.CHITRAL,
  Region.NARAN,
  Region.KAGHAN,
  Region.MURREE,
  Region.ABBOTTABAD,
  Region.NEELUM_VALLEY,
  Region.MUZAFFARABAD,
  Region.RAWALAKOT
];